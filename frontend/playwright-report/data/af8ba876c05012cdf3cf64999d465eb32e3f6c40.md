# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: alerts/alerts.spec.ts >> 告警规则管理 >> 编辑告警规则
- Location: e2e/alerts/alerts.spec.ts:196:3

# Error details

```
Test timeout of 60000ms exceeded.
```

```
Error: page.click: Test timeout of 60000ms exceeded.
Call log:
  - waiting for locator('[data-testid="add-rule-btn"]')

```

# Test source

```ts
  98  |   
  99  |   test('告警清除', async ({ adminPage }) => {
  100 |     await adminPage.goto('/alerts');
  101 |     
  102 |     // 检查清除按钮
  103 |     const clearBtn = adminPage.locator('[data-testid="clear-btn"], button:has-text("清除"), button:has-text("Clear")');
  104 |     
  105 |     if (await clearBtn.isVisible()) {
  106 |       await clearBtn.click();
  107 |       
  108 |       // 确认清除操作
  109 |       const confirmDialog = adminPage.locator('[role="dialog"], [data-testid="confirm-dialog"]');
  110 |       if (await confirmDialog.isVisible()) {
  111 |         await confirmDialog.locator('button:has-text("确认"), button:has-text("Confirm")').click();
  112 |       }
  113 |       
  114 |       // 验证告警已清除
  115 |       await expect(adminPage.locator('text=/暂无告警|No alerts/i')).toBeVisible({ timeout: 5000 });
  116 |     }
  117 |   });
  118 |   
  119 |   test('告警列表筛选', async ({ adminPage }) => {
  120 |     // 触发多个告警
  121 |     await sendMockTelemetry(TEST_DEVICES.cnc.id, { temperature: 150 });
  122 |     await sendMockTelemetry(TEST_DEVICES.inj.id, { vibration: 8 });
  123 |     await adminPage.waitForTimeout(3000);
  124 |     
  125 |     await adminPage.goto('/alerts');
  126 |     
  127 |     // 筛选严重告警
  128 |     const severityFilter = adminPage.locator('[data-testid="severity-filter"], select[name="severity"]');
  129 |     if (await severityFilter.isVisible()) {
  130 |       await severityFilter.selectOption('critical');
  131 |       await adminPage.waitForTimeout(500);
  132 |       
  133 |       // 验证只显示严重告警
  134 |       const alerts = adminPage.locator('[data-testid="alert-row"]');
  135 |       const count = await alerts.count();
  136 |       
  137 |       for (let i = 0; i < count; i++) {
  138 |         const severity = await alerts.nth(i).locator('[data-testid="severity"]').textContent();
  139 |         expect(severity?.toLowerCase()).toContain('critical');
  140 |       }
  141 |     }
  142 |   });
  143 |   
  144 |   test('告警统计概览', async ({ adminPage }) => {
  145 |     await adminPage.goto('/alerts');
  146 |     
  147 |     // 验证统计卡片存在
  148 |     const statsCards = adminPage.locator('[data-testid="alert-stats"]');
  149 |     
  150 |     if (await statsCards.isVisible()) {
  151 |       // 验证各类告警计数
  152 |       await expect(statsCards.locator('text=/严重|Critical/i')).toBeVisible();
  153 |       await expect(statsCards.locator('text=/警告|Warning/i')).toBeVisible();
  154 |       await expect(statsCards.locator('text=/信息|Info/i')).toBeVisible();
  155 |       
  156 |       // 验证总数
  157 |       const totalCount = await statsCards.locator('[data-testid="total-count"]').textContent();
  158 |       expect(parseInt(totalCount || '0')).toBeGreaterThanOrEqual(0);
  159 |     }
  160 |   });
  161 | });
  162 | 
  163 | test.describe('告警规则管理', () => {
  164 |   
  165 |   test.beforeEach(async ({ adminPage }) => {
  166 |     await adminPage.goto('/alerts/rules');
  167 |   });
  168 |   
  169 |   test('显示告警规则列表', async ({ adminPage }) => {
  170 |     // 验证页面标题
  171 |     await expect(adminPage.locator('h1, h2').filter({ hasText: /规则|Rules/i })).toBeVisible();
  172 |     
  173 |     // 验证规则列表存在
  174 |     const rulesList = adminPage.locator('[data-testid="rules-list"], table');
  175 |     await expect(rulesList).toBeVisible();
  176 |   });
  177 |   
  178 |   test('添加告警规则', async ({ adminPage }) => {
  179 |     // 点击添加规则按钮
  180 |     await adminPage.click('[data-testid="add-rule-btn"], button:has-text("添加规则")');
  181 |     
  182 |     // 填写规则信息
  183 |     await adminPage.fill('[name="rule-name"]', 'Test Temperature Rule');
  184 |     await adminPage.selectOption('[name="device-type"]', 'CNC');
  185 |     await adminPage.selectOption('[name="metric"]', 'temperature');
  186 |     await adminPage.fill('[name="threshold"]', '100');
  187 |     await adminPage.selectOption('[name="severity"]', 'warning');
  188 |     
  189 |     // 提交
  190 |     await adminPage.click('button[type="submit"]');
  191 |     
  192 |     // 验证规则出现在列表中
  193 |     await expect(adminPage.locator('text="Test Temperature Rule"')).toBeVisible({ timeout: 10000 });
  194 |   });
  195 |   
  196 |   test('编辑告警规则', async ({ adminPage }) => {
  197 |     // 创建测试规则
> 198 |     await adminPage.click('[data-testid="add-rule-btn"]');
      |                     ^ Error: page.click: Test timeout of 60000ms exceeded.
  199 |     await adminPage.fill('[name="rule-name"]', 'Edit Test Rule');
  200 |     await adminPage.fill('[name="threshold"]', '100');
  201 |     await adminPage.click('button[type="submit"]');
  202 |     await expect(adminPage.locator('text="Edit Test Rule"')).toBeVisible({ timeout: 10000 });
  203 |     
  204 |     // 点击编辑按钮
  205 |     await adminPage.click('tr:has-text("Edit Test Rule") [data-testid="edit-btn"]');
  206 |     
  207 |     // 修改阈值
  208 |     await adminPage.fill('[name="threshold"]', '120');
  209 |     await adminPage.click('button[type="submit"]');
  210 |     
  211 |     // 验证更新成功
  212 |     await expect(adminPage.locator('text="120"')).toBeVisible({ timeout: 5000 });
  213 |   });
  214 |   
  215 |   test('删除告警规则', async ({ adminPage }) => {
  216 |     // 创建测试规则
  217 |     await adminPage.click('[data-testid="add-rule-btn"]');
  218 |     await adminPage.fill('[name="rule-name"]', 'Delete Test Rule');
  219 |     await adminPage.click('button[type="submit"]');
  220 |     await expect(adminPage.locator('text="Delete Test Rule"')).toBeVisible({ timeout: 10000 });
  221 |     
  222 |     // 删除规则
  223 |     await adminPage.click('tr:has-text("Delete Test Rule") [data-testid="delete-btn"]');
  224 |     await adminPage.click('[role="dialog"] button:has-text("确认")');
  225 |     
  226 |     // 验证规则已删除
  227 |     await expect(adminPage.locator('text="Delete Test Rule"')).not.toBeVisible({ timeout: 5000 });
  228 |   });
  229 |   
  230 |   test('启用/禁用规则', async ({ adminPage }) => {
  231 |     // 检查规则开关
  232 |     const toggleSwitch = adminPage.locator('[data-testid="rule-row"]').first().locator('[data-testid="toggle"], input[type="checkbox"]');
  233 |     
  234 |     if (await toggleSwitch.isVisible()) {
  235 |       // 获取当前状态
  236 |       const isEnabled = await toggleSwitch.isChecked();
  237 |       
  238 |       // 切换状态
  239 |       await toggleSwitch.click();
  240 |       
  241 |       // 验证状态变化
  242 |       await adminPage.waitForTimeout(500);
  243 |       expect(await toggleSwitch.isChecked()).toBe(!isEnabled);
  244 |     }
  245 |   });
  246 | });
  247 | 
  248 | test.describe('告警通知设置', () => {
  249 |   
  250 |   test.beforeEach(async ({ adminPage }) => {
  251 |     await adminPage.goto('/settings/notifications');
  252 |   });
  253 |   
  254 |   test('邮件通知设置', async ({ adminPage }) => {
  255 |     // 检查邮件通知开关
  256 |     const emailToggle = adminPage.locator('[data-testid="email-notification"], input[type="checkbox"]');
  257 |     
  258 |     if (await emailToggle.isVisible()) {
  259 |       await emailToggle.check();
  260 |       
  261 |       // 填写邮箱地址
  262 |       const emailInput = adminPage.locator('[name="email"]');
  263 |       await emailInput.fill('admin@example.com');
  264 |       
  265 |       // 保存设置
  266 |       await adminPage.click('button:has-text("保存")');
  267 |       
  268 |       // 验证保存成功
  269 |       await expect(adminPage.locator('text=/保存成功|Saved/i')).toBeVisible({ timeout: 5000 });
  270 |     }
  271 |   });
  272 |   
  273 |   test('通知级别设置', async ({ adminPage }) => {
  274 |     // 选择通知级别
  275 |     const levelSelect = adminPage.locator('[name="notification-level"]');
  276 |     if (await levelSelect.isVisible()) {
  277 |       await levelSelect.selectOption('critical'); // 只通知严重告警
  278 |       
  279 |       await adminPage.click('button:has-text("保存")');
  280 |       await expect(adminPage.locator('text=/保存成功/i')).toBeVisible({ timeout: 5000 });
  281 |     }
  282 |   });
  283 | });
```